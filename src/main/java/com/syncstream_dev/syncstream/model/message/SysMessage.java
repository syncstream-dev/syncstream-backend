package com.syncstream_dev.syncstream.model.message;

public class SysMessage extends Message {
    public SysMessage(String messageId, long timestamp, String content) {
        super(messageId, timestamp, content);
    }
}
